{
  "buildCommand": "npm install --prefix frontend && npm run build && npm install --prefix backend --legacy-peer-deps --omit=dev",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.js": {
      "includeFiles": "backend/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
